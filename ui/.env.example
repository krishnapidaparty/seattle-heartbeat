# ── LLM Provider (@copilot) ────────────────────────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# ── Bridge Mode ────────────────────────────────────────────────────
# Mode: "standalone" | "openclaw" | "hybrid"
#   standalone — CopilotKit + your LLM only, no OpenClaw dependency (default)
#   openclaw   — OpenClaw controls everything via clawg-ui (AG-UI protocol)
#   hybrid     — @copilot uses your LLM, @clawpilot uses OpenClaw via clawg-ui
CLAWPILOT_MODE=standalone

# ── Client-side mode (must match CLAWPILOT_MODE) ─────────────────
NEXT_PUBLIC_CLAWPILOT_MODE=standalone

# ── OpenClaw Connection (required for openclaw / hybrid modes) ────
# Points to the clawg-ui AG-UI endpoint on your OpenClaw gateway.
# Requires the clawg-ui plugin: openclaw plugins install @contextableai/clawg-ui
# OPENCLAW_AGENT_URL=http://localhost:18789/v1/clawg-ui
# OPENCLAW_AGENT_TOKEN=your-device-pairing-token

# ── Server-side tools ────────────────────────────────────────────
# Base URL the bridge tools use to call your app's API routes.
# APP_URL=http://localhost:3000
# ── Relays ─────────────────────────────────────────────────────
NEXT_PUBLIC_RELAY_SERVICE_URL=http://localhost:4001

